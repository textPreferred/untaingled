pre-commit:
  commands:
    lint:
      run: bun run lint
      stage_fixed: true
    format:
      glob: "*.{js,ts,jsx,tsx,vue,json,md,yaml,yml}"
      exclude: "*lock*"
      run: bun run format {staged_files}
      stage_fixed: true
