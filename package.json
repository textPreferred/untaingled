{
  "name": "untaingled",
  "version": "1.0.0",
  "description": "Untangling personal histories",
  "license": "MIT",
  "scripts": {
    "build": "vite build && tsc && tsc -p tsconfig.client.json --noEmit",
    "dev": "vite build && bun src/index.ts",
    "test": "bun test --pass-with-no-tests src/",
    "test:coverage": "bun test --pass-with-no-tests --coverage --coverage-reporter=lcov src/",
    "lint": "oxlint --fix .",
    "lint:check": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "e2e": "playwright test",
    "build:client": "vite build",
    "build:docker": "docker build -t untaingled ."
  },
  "dependencies": {
    "hono": "^4.12.19",
    "knex": "^3.2.10",
    "sqlite3": "^6.0.1",
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/bun": "^1.3.13",
    "@types/node": "^25.8.0",
    "@vitejs/plugin-vue": "^6.0.7",
    "lefthook": "^2.1.8",
    "oxfmt": "^0.50.0",
    "oxlint": "^1.61.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.13"
  }
}
